# Локальная разработка: не задавайте VITE_API_URL — запросы идут через прокси Vite
# на /api → бэкенд (по умолчанию http://127.0.0.1:8080). При необходимости:
# VITE_PROXY_TARGET=http://127.0.0.1:8080
#
# Сборка / прод: полный URL API (без завершающего слэша)
# VITE_API_URL=https://api.example.com
